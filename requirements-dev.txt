# Development dependencies — testing, linting, formatting, type checking.
# Install alongside the core requirements:
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest>=7.2.0
pytest-cov>=4.0.0
black>=22.0.0
flake8>=5.0.0
isort>=5.10.0
mypy>=1.0.0
