# Core runtime dependencies — required by the shipped scripts.
# These are the only third-party packages the scripts under scripts/ import.
requests>=2.28.0
urllib3>=1.26.0

# JWT analysis (scripts/authentication/jwt_analyzer.py).
# cryptography is pulled in by PyJWT for RS*/ES* algorithm support.
PyJWT>=2.6.0
cryptography>=3.4.0

# Dynamic instrumentation (scripts/dynamic_analysis/frida_automation.py).
frida>=16.0.0
frida-tools>=12.0.0

# Development and optional extras live in separate files:
#   pip install -r requirements-dev.txt        # tests, linting, type checks
#   pip install -r requirements-optional.txt   # extras for not-yet-shipped features
